apiVersion: v1
kind: Service
metadata:
  name: {{ include "marvel-watchlist.fullname" . }}
  labels:
    {{- include "marvel-watchlist.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "marvel-watchlist.selectorLabels" . | nindent 4 }}
